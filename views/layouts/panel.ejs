<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= typeof title !== 'undefined' ? title : 'Panel' %> | Key Distribution</title>
  <style>
    * { box-sizing: border-box; }
    body { font-family: system-ui, sans-serif; margin: 0; background: #0f0f12; color: #e4e4e7; min-height: 100vh; }
    .container { max-width: 900px; margin: 0 auto; padding: 1.5rem; }
    h1, h2 { color: #fafafa; margin-top: 0; }
    a { color: #60a5fa; text-decoration: none; }
    a:hover { text-decoration: underline; }
    .card { background: #18181b; border-radius: 8px; padding: 1.25rem; margin-bottom: 1rem; border: 1px solid #27272a; }
    .btn { display: inline-block; padding: 0.5rem 1rem; background: #3b82f6; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 0.9rem; }
    .btn:hover { background: #2563eb; }
    .btn-danger { background: #dc2626; }
    .btn-danger:hover { background: #b91c1c; }
    .btn-success { background: #16a34a; }
    .btn-success:hover { background: #15803d; }
    input, select, textarea { padding: 0.5rem; border: 1px solid #3f3f46; border-radius: 6px; background: #27272a; color: #e4e4e7; width: 100%; max-width: 300px; }
    label { display: block; margin-bottom: 0.25rem; color: #a1a1aa; font-size: 0.875rem; }
    .form-group { margin-bottom: 1rem; }
    .nav { display: flex; gap: 1rem; margin-bottom: 1.5rem; padding-bottom: 1rem; border-bottom: 1px solid #27272a; }
    .error { color: #f87171; margin-bottom: 1rem; }
    .success { color: #4ade80; margin-bottom: 1rem; }
    table { width: 100%; border-collapse: collapse; }
    th, td { padding: 0.5rem; text-align: left; border-bottom: 1px solid #27272a; }
    th { color: #a1a1aa; font-weight: 500; }
  </style>
</head>
<body>
  <div class="container">
    <% if (typeof nav !== 'undefined' && nav) { %>
    <nav class="nav">
      <a href="/panel/seller">Dashboard</a>
      <a href="/panel/seller/credits">Credits</a>
      <a href="/panel/seller/plans">Plans</a>
      <a href="/panel/seller/keys">Keys</a>
      <form action="/panel/seller/logout" method="post" style="margin-left: auto;">
        <button type="submit" class="btn">Logout</button>
      </form>
    </nav>
    <% } %>
    <% if (typeof adminNav !== 'undefined' && adminNav) { %>
    <nav class="nav">
      <a href="/panel/admin">Dashboard</a>
      <a href="/panel/admin/sellers/new">New Seller</a>
      <form action="/panel/admin/logout" method="post" style="margin-left: auto;">
        <button type="submit" class="btn">Logout</button>
      </form>
    </nav>
    <% } %>
    <%- body %>
  </div>
</body>
</html>
