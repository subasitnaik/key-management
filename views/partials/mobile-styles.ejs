<style>
  *, *::before, *::after { box-sizing: border-box; }
  html { -webkit-text-size-adjust: 100%; }
  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    margin: 0;
    background: #0f0f12;
    color: #e4e4e7;
    min-height: 100vh;
    padding: 1rem;
    padding-left: max(1rem, env(safe-area-inset-left));
    padding-right: max(1rem, env(safe-area-inset-right));
    padding-bottom: max(1rem, env(safe-area-inset-bottom));
    font-size: 15px;
    line-height: 1.5;
  }
  .container { width: 100%; max-width: 100%; margin: 0 auto; }
  .card {
    background: #18181b;
    border-radius: 12px;
    padding: 1rem;
    margin-bottom: 1rem;
    border: 1px solid #27272a;
  }
  h1, h2 { color: #fafafa; margin: 0 0 0.75rem 0; font-size: 1.25rem; }
  h1 { font-size: 1.5rem; }
  a { color: #60a5fa; text-decoration: none; -webkit-tap-highlight-color: transparent; }
  a:active { opacity: 0.8; }
  .btn {
    display: inline-block;
    padding: 0.75rem 1rem;
    min-height: 44px;
    min-width: 44px;
    background: #3b82f6;
    color: white;
    border: none;
    border-radius: 10px;
    cursor: pointer;
    font-size: 0.95rem;
    text-decoration: none;
    text-align: center;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }
  .btn:active { opacity: 0.9; }
  .btn-danger { background: #dc2626; }
  .btn-success { background: #16a34a; }
  .btn-block { display: block; width: 100%; }
  .success { color: #4ade80; margin-bottom: 0.75rem; }
  .error { color: #f87171; margin-bottom: 0.75rem; }
  input, select, textarea {
    padding: 0.75rem;
    min-height: 44px;
    border: 1px solid #3f3f46;
    border-radius: 10px;
    background: #27272a;
    color: #e4e4e7;
    width: 100%;
    font-size: 16px;
  }
  input:focus, select:focus, textarea:focus { outline: 2px solid #3b82f6; outline-offset: 2px; }
  label { display: block; margin-bottom: 0.35rem; color: #a1a1aa; font-size: 0.875rem; }
  .form-group { margin-bottom: 1rem; }
  .nav {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid #27272a;
  }
  .nav a, .nav .btn { padding: 0.5rem 0.75rem; min-height: 44px; }
  .nav form { margin-left: auto; }
  .table-wrap { overflow-x: auto; -webkit-overflow-scrolling: touch; margin: 0 -0.5rem; }
  table { width: 100%; min-width: 320px; border-collapse: collapse; font-size: 0.9rem; }
  th, td { padding: 0.6rem 0.5rem; text-align: left; border-bottom: 1px solid #27272a; }
  th { color: #a1a1aa; font-weight: 500; }
  .row-card { display: block; padding: 0.75rem; border-bottom: 1px solid #27272a; }
  .row-card:last-child { border-bottom: none; }
  .row-card .label { color: #a1a1aa; font-size: 0.75rem; }
  .row-card .value { margin-bottom: 0.35rem; }
  .row-card .value:last-child { margin-bottom: 0; }
  .btn-group { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: 0.5rem; }
  .btn-group .btn { flex: 1; min-width: 80px; }
  code { background: #27272a; padding: 0.2rem 0.4rem; border-radius: 4px; font-size: 0.85em; word-break: break-all; }
  .stats { display: grid; grid-template-columns: repeat(2, 1fr); gap: 0.5rem; margin-bottom: 1rem; }
  .stat { background: #27272a; padding: 0.75rem; border-radius: 8px; text-align: center; }
  .stat .val { font-size: 1.25rem; font-weight: 600; color: #fafafa; }
  .stat .lbl { font-size: 0.75rem; color: #a1a1aa; }
  @media (min-width: 640px) {
    body { padding: 1.5rem; }
    .container { max-width: 640px; }
    .card { padding: 1.25rem; }
    .nav { gap: 0.75rem; }
  }
  @media (min-width: 900px) {
    .container { max-width: 900px; }
    .stats { grid-template-columns: repeat(4, 1fr); }
  }
</style>
