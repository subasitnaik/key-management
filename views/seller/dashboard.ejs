<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Seller Dashboard</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="header">
    <input type="checkbox" id="nav-toggle" class="nav-toggle" aria-hidden="true">
    <label for="nav-toggle" class="hamburger" aria-label="Open menu"><span></span></label>
    <a href="/panel/seller/logout" class="header-logout">Logout</a>
    <nav class="nav-menu">
      <a href="/panel/seller">Dashboard</a>
      <a href="/panel/seller/keys">Keys</a>
      <a href="/panel/seller/plans">Plans</a>
      <a href="/panel/seller/payments">Payments</a>
    </nav>
    <label for="nav-toggle" class="nav-backdrop" aria-label="Close menu"></label>
  </header>
  <div class="page">
    <h1>Seller Dashboard</h1>
    <div class="card">
      <p><strong>Credits:</strong> <%= creditsBalance %></p>
      <p><strong>CCPU:</strong> <%= typeof ccpu !== 'undefined' ? ccpu : 30 %> (credits per user per month)</p>
      <p><strong>Active users:</strong> <%= typeof activeUsers !== 'undefined' ? activeUsers : 0 %> (keys not expired)</p>
      <p><strong>Earned this cycle:</strong> ₹<%= typeof earnedAmount !== 'undefined' ? earnedAmount.toFixed(2) : '0.00' %></p>
    </div>
    <div class="card">
      <h2>Connect link (paste in your tool)</h2>
      <% const isUrl = typeof connectLink !== 'undefined' && connectLink && (connectLink.indexOf('http://') === 0 || connectLink.indexOf('https://') === 0); %>
      <% if (isUrl) { %>
      <p><code class="badge connect-link"><%= connectLink %></code></p>
      <p class="text-dim">Copy this URL and paste it in your Android tool config so keys work.</p>
      <% } else { %>
      <p class="text-dim">Your connect path: <code>/connect/<%= typeof slug !== 'undefined' ? slug : '' %></code>. Paste your site URL + this path in the tool (e.g. https://your-site.vercel.app/connect/<%= typeof slug !== 'undefined' ? slug : '' %>).</p>
      <% } %>
    </div>
    <div class="card">
      <form method="post" action="/panel/seller/maintenance">
        <button type="submit" class="btn btn-secondary">Toggle Maintenance Mode</button>
      </form>
      <form method="post" action="/panel/seller/reset" style="margin-top:0.5rem;" onsubmit="return confirm('Reset all subscriptions for this seller? This cannot be undone.');">
        <button type="submit" class="btn btn-danger">Reset Cycle</button>
      </form>
    </div>
  </div>
</body>
</html>
