<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Seller Dashboard</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="header">
    <input type="checkbox" id="nav-toggle" class="nav-toggle" aria-hidden="true">
    <label for="nav-toggle" class="hamburger" aria-label="Open menu"><span></span></label>
    <a href="/panel/seller/logout" class="header-logout">Logout</a>
    <nav class="nav-menu">
      <a href="/panel/seller">Dashboard</a>
      <a href="/panel/seller/keys">Keys</a>
      <a href="/panel/seller/plans">Plans</a>
      <a href="/panel/seller/payments">Payments</a>
    </nav>
    <label for="nav-toggle" class="nav-backdrop" aria-label="Close menu"></label>
  </header>
  <div class="page">
    <h1>Seller Dashboard</h1>
    <section class="stats-grid" aria-label="Overview stats">
      <div class="stat-card">
        <span class="stat-label">Credits</span>
        <span class="stat-value"><%= creditsBalance %></span>
      </div>
      <div class="stat-card">
        <span class="stat-label">CCPU</span>
        <span class="stat-value"><%= typeof ccpu !== 'undefined' ? ccpu : 30 %></span>
        <span class="stat-meta">credits per user / month</span>
      </div>
      <div class="stat-card">
        <span class="stat-label">Active users</span>
        <span class="stat-value"><%= typeof activeUsers !== 'undefined' ? activeUsers : 0 %></span>
        <span class="stat-meta">keys not expired</span>
      </div>
      <div class="stat-card stat-card-earned">
        <span class="stat-label">Earned this cycle</span>
        <span class="stat-value">₹<%= typeof earnedAmount !== 'undefined' ? earnedAmount.toFixed(2) : '0.00' %></span>
      </div>
    </section>
    <div class="card">
      <h2>Connect link (paste in your tool)</h2>
      <% const isUrl = typeof connectLink !== 'undefined' && connectLink && (connectLink.indexOf('http://') === 0 || connectLink.indexOf('https://') === 0); %>
      <% if (isUrl) { %>
      <div class="connect-link-row">
        <code class="badge connect-link" id="connect-link-text"><%= connectLink %></code>
        <button type="button" class="btn btn-small btn-copy" id="copy-connect-link" data-url="<%= connectLink %>" aria-label="Copy link">Copy</button>
      </div>
      <p class="text-dim" id="copy-feedback" style="display:none;">Copied!</p>
      <p class="text-dim">Paste this URL in your Android tool config so keys work.</p>
      <% if (typeof usedPanelUrl !== 'undefined' && !usedPanelUrl) { %>
      <p class="text-dim warning-inline">To always show your production link (e.g. key-management-five.vercel.app), set <strong>PANEL_URL</strong> in Vercel → Project → Settings → Environment Variables to <code>https://key-management-five.vercel.app</code></p>
      <% } %>
      <% } else { %>
      <p class="text-dim">Your connect path: <code>/connect/<%= typeof slug !== 'undefined' ? slug : '' %></code>. Set <strong>PANEL_URL</strong> in Vercel to your production URL (e.g. <code>https://key-management-five.vercel.app</code>), then the full link will appear here.</p>
      <% } %>
    </div>
    <script>
      (function() {
        var btn = document.getElementById('copy-connect-link');
        var feedback = document.getElementById('copy-feedback');
        if (btn && feedback) {
          btn.addEventListener('click', function() {
            var url = btn.getAttribute('data-url') || (document.getElementById('connect-link-text') && document.getElementById('connect-link-text').textContent) || '';
            if (!url) return;
            if (navigator.clipboard && navigator.clipboard.writeText) {
              navigator.clipboard.writeText(url).then(function() {
                feedback.style.display = 'block';
                feedback.textContent = 'Copied!';
                setTimeout(function() { feedback.style.display = 'none'; }, 2000);
              }).catch(function() { feedback.textContent = 'Copy failed'; feedback.style.display = 'block'; });
            } else {
              var ta = document.createElement('textarea');
              ta.value = url;
              ta.setAttribute('readonly', '');
              ta.style.position = 'fixed';
              ta.style.left = '-9999px';
              document.body.appendChild(ta);
              ta.select();
              try {
                document.execCommand('copy');
                feedback.style.display = 'block';
                feedback.textContent = 'Copied!';
                setTimeout(function() { feedback.style.display = 'none'; }, 2000);
              } catch (e) { feedback.textContent = 'Copy failed'; feedback.style.display = 'block'; }
              document.body.removeChild(ta);
            }
          });
        }
      })();
    </script>
    <% if (typeof settingsSaved !== 'undefined' && settingsSaved) { %>
    <div class="card success-msg">Settings saved.</div>
    <% } %>
    <div class="card">
      <h3>Panel settings (Free Source compatible)</h3>
      <form method="post" action="/panel/seller" class="settings-form">
        <input type="hidden" name="_action" value="settings" />
        <div class="form-group">
          <label for="mod_name">Mod name</label>
          <input type="text" id="mod_name" name="mod_name" value="<%= typeof modName !== 'undefined' ? (modName || '') : '' %>" placeholder="e.g. VIP MOD" />
        </div>
        <div class="form-group">
          <label for="maintenance_message">Maintenance message (shown when maintenance is on)</label>
          <input type="text" id="maintenance_message" name="maintenance_message" value="<%= typeof maintenanceMessage !== 'undefined' ? (maintenanceMessage || '') : '' %>" placeholder="e.g. Under maintenance, try again later" />
        </div>
        <button type="submit" class="btn">Save settings</button>
      </form>
    </div>
    <div class="card">
      <div class="maintenance-row">
        <span class="maintenance-label">Maintenance mode</span>
        <form method="post" action="/panel/seller" class="maintenance-form">
          <input type="hidden" name="_action" value="maintenance" />
          <% const maintenanceOn = typeof maintenanceMode !== 'undefined' && maintenanceMode; %>
          <div class="toggle-wrap">
            <span class="toggle-text toggle-off<%= maintenanceOn ? '' : ' active' %>">Off</span>
            <button type="submit" class="toggle-slider<%= maintenanceOn ? ' is-on' : '' %>" role="switch" aria-checked="<%= maintenanceOn ? 'true' : 'false' %>" aria-label="Maintenance mode">
              <span class="toggle-thumb"></span>
            </button>
            <span class="toggle-text toggle-on<%= maintenanceOn ? ' active' : '' %>">On</span>
          </div>
        </form>
      </div>
      <form method="post" action="/panel/seller" class="reset-form" onsubmit="return confirm('Reset all subscriptions for this seller? This cannot be undone.');">
        <input type="hidden" name="_action" value="reset" />
        <button type="submit" class="btn btn-danger">Reset Cycle</button>
      </form>
    </div>
  </div>
</body>
</html>
