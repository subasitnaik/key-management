<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Keys</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="header">
    <input type="checkbox" id="nav-toggle" class="nav-toggle" aria-hidden="true">
    <label for="nav-toggle" class="hamburger" aria-label="Open menu"><span></span></label>
    <a href="/panel/seller/logout" class="header-logout">Logout</a>
    <nav class="nav-menu">
      <a href="/panel/seller">Dashboard</a>
      <a href="/panel/seller/keys">Keys</a>
      <a href="/panel/seller/plans">Plans</a>
      <a href="/panel/seller/payments">Payments</a>
    </nav>
    <label for="nav-toggle" class="nav-backdrop" aria-label="Close menu"></label>
  </header>
  <div class="page">
    <h1>Keys / Subscriptions</h1>
    <p><a href="/panel/seller" class="btn btn-secondary">Back</a> <a href="/panel/seller/keys/generate" class="btn">Generate key</a></p>
    <% if (typeof generated !== 'undefined' && generated) { %>
    <div class="card success-msg">
      Key generated.<% if (typeof generatedKey !== 'undefined' && generatedKey) { %> <strong>Key:</strong> <code class="badge"><%= generatedKey %></code> — copy and share with the user.<% } %>
    </div>
    <% } %>
    <div class="card">
      <ul class="list">
        <% (subscriptions || []).forEach(function(s) { %>
        <li>
          <span>Key: <code class="badge"><%= s.key %></code> — max devices: <%= s.max_devices ?? 1 %> — expires <%= s.expires_at %></span>
        </li>
        <% }); %>
      </ul>
    </div>
  </div>
</body>
</html>
