<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>Edit Plan</title>
  <%- include('../partials/mobile-styles') %>
</head>
<body>
  <div class="container">
    <%- include('../partials/seller-nav') %>
    <h1>Edit Plan</h1>
    <% if (typeof err !== 'undefined' && err === 'missing') { %><p class="error">Fill all fields</p><% } %>
    <div class="card">
      <form method="post" action="/panel/seller/plans/<%= plan.id %>">
        <div class="form-group">
          <label>Name</label>
          <input type="text" name="name" value="<%= plan.name %>" placeholder="7 Days" required>
        </div>
        <div class="form-group">
          <label>Days</label>
          <input type="number" name="days" value="<%= plan.days %>" min="1" required>
        </div>
        <div class="form-group">
          <label>Price (₹)</label>
          <input type="number" name="price" value="<%= plan.price %>" min="0" step="0.01" required>
        </div>
        <div class="btn-group">
          <button type="submit" class="btn">Save</button>
          <a href="/panel/seller/plans" class="btn">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
