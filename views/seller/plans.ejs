<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Plans</title>
</head>
<body>
  <h1>Plans</h1>
  <p><a href="/panel/seller">Back</a></p>
  <ul>
    <% (plans || []).forEach(function(p) { %>
    <li><%= p.name %> — <%= p.days %> days — ₹<%= p.price %></li>
    <% }); %>
  </ul>
</body>
</html>
