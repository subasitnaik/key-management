<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Generate Key</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="header">
    <input type="checkbox" id="nav-toggle" class="nav-toggle" aria-hidden="true">
    <label for="nav-toggle" class="hamburger" aria-label="Open menu"><span></span></label>
    <a href="/panel/seller/logout" class="header-logout">Logout</a>
    <nav class="nav-menu">
      <a href="/panel/seller">Dashboard</a>
      <a href="/panel/seller/keys">Keys</a>
      <a href="/panel/seller/plans">Plans</a>
      <a href="/panel/seller/payments">Payments</a>
    </nav>
    <label for="nav-toggle" class="nav-backdrop" aria-label="Close menu"></label>
  </header>
  <div class="page">
    <h1>Generate Key</h1>
    <p><a href="/panel/seller/keys" class="btn btn-secondary">Back</a></p>
    <% if (typeof error !== 'undefined' && error) { %>
    <div class="card error-msg" role="alert">
      <% if (error === 'duplicate') { %>That key already exists. Use a different key or generate random.<% } else { %>Could not generate. Try again.<% } %>
    </div>
    <% } %>
    <div class="card">
      <form method="post" action="/panel/seller/keys/generate">
        <div class="form-group">
          <label>Key type</label>
          <select name="key_type" id="key-type">
            <option value="random">Random</option>
            <option value="custom">Custom</option>
          </select>
        </div>
        <div class="form-group" id="custom-key-group" style="display:none;">
          <label>Custom key</label>
          <input name="custom_key" type="text" placeholder="Enter your key string">
        </div>
        <div class="form-group" id="key-name-group">
          <label>Key name <span class="text-dim">(optional, for random keys)</span></label>
          <input name="key_name" type="text" placeholder="e.g. TESTER" maxlength="32" title="Optional: letters, numbers, underscore (others stripped)">
          <p class="text-dim" style="margin-top:0.25rem;font-size:0.85rem;">If set, key will look like: <code>8DxTESTERx&lt;random&gt;</code>. If empty: <code>8Dx&lt;random&gt;</code>.</p>
        </div>
        <div class="form-group">
          <label>Max devices</label>
          <input name="max_devices" type="number" value="1" min="1" required title="Number of devices this key can be used on">
        </div>
        <div class="form-group">
          <label>Valid for (days)</label>
          <input name="days" type="number" value="30" min="1" required>
        </div>
        <button type="submit" class="btn">Generate key</button>
      </form>
    </div>
    <script>
      (function() {
        var keyType = document.getElementById('key-type');
        var customGroup = document.getElementById('custom-key-group');
        var nameGroup = document.getElementById('key-name-group');
        function update() {
          var isCustom = keyType.value === 'custom';
          customGroup.style.display = isCustom ? 'block' : 'none';
          nameGroup.style.display = isCustom ? 'none' : 'block';
        }
        keyType.addEventListener('change', update);
        update();
      })();
    </script>
  </div>
</body>
</html>
