<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>Reset Cycle</title>
  <%- include('../partials/mobile-styles') %>
</head>
<body>
  <div class="container">
    <%- include('../partials/seller-nav') %>
    <h1>Reset Cycle</h1>
    <p class="error"><strong>Warning:</strong> This will delete ALL keys and subscriptions. Keys will stop working immediately.</p>
    <% if (step === 1) { %>
    <div class="card">
      <p>Type <strong>RESET</strong> to confirm:</p>
      <% if (typeof err !== 'undefined' && err === 'confirm') { %><p class="error">Type RESET exactly</p><% } %>
      <form method="post" action="/panel/seller/reset">
        <input type="hidden" name="step" value="1">
        <div class="form-group">
          <input type="text" name="confirm" placeholder="RESET" required>
        </div>
        <button type="submit" class="btn btn-danger btn-block">Continue</button>
      </form>
    </div>
    <% } else if (step === 2) { %>
    <div class="card">
      <p>Type <strong>YES</strong> to finalize:</p>
      <% if (typeof err !== 'undefined' && err === 'confirm2') { %><p class="error">Type YES exactly</p><% } %>
      <form method="post" action="/panel/seller/reset">
        <input type="hidden" name="step" value="2">
        <div class="form-group">
          <input type="text" name="confirm" placeholder="YES" required>
        </div>
        <button type="submit" class="btn btn-danger btn-block">Reset Cycle</button>
      </form>
    </div>
    <% } %>
    <p><a href="/panel/seller">← Cancel</a></p>
  </div>
</body>
</html>
