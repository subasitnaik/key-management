<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>New Seller</title>
  <%- include('../partials/mobile-styles') %>
  <style>
    .checkbox-wrap { display: flex; align-items: center; gap: 0.5rem; min-height: 44px; }
    .checkbox-wrap input { width: auto; min-height: auto; }
  </style>
</head>
<body>
  <div class="container">
    <nav class="nav">
      <a href="/panel/admin">Dashboard</a>
      <a href="/panel/admin/sellers/new">New Seller</a>
      <a href="/panel/admin/settings">Change Password</a>
      <form action="/panel/admin/logout" method="post">
        <button type="submit" class="btn">Logout</button>
      </form>
    </nav>
    <h1>New Seller</h1>
    <% if (typeof err !== 'undefined' && err === 'missing') { %><p class="error">Fill all required fields</p><% } %>
    <div class="card">
      <form method="post" action="/panel/admin/sellers">
        <div class="form-group">
          <label>Slug (for connect URL: /connect/slug/)</label>
          <input type="text" name="slug" placeholder="myseller" required>
        </div>
        <div class="form-group">
          <label>Username</label>
          <input type="text" name="username" placeholder="seller1" required>
        </div>
        <div class="form-group">
          <label>Password</label>
          <input type="password" name="password" placeholder="••••••••" required>
        </div>
        <div class="form-group">
          <label>CCPU (credits per 30-day month)</label>
          <input type="number" name="ccpu" value="30" min="1">
        </div>
        <div class="form-group">
          <label>Telegram Bot Token</label>
          <input type="password" name="telegram_bot_token" value="" placeholder="123456:ABC-DEF... (create via @BotFather, then paste here)" autocomplete="off">
          <span class="label" style="font-size:0.75rem;">Only Master Admin can assign bots. Create bot via @BotFather, get token, paste here.</span>
        </div>
        <div class="form-group checkbox-wrap">
          <input type="checkbox" name="query_channel" value="1" id="qc">
          <label for="qc" style="margin:0;">Enable query channel</label>
        </div>
        <button type="submit" class="btn btn-block">Create</button>
      </form>
    </div>
  </div>
</body>
</html>
