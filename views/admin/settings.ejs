<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>System Settings</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="header">
    <input type="checkbox" id="nav-toggle" class="nav-toggle" aria-hidden="true">
    <label for="nav-toggle" class="hamburger" aria-label="Open menu"><span></span></label>
    <a href="/panel/admin/logout" class="header-logout">Logout</a>
    <nav class="nav-menu">
      <a href="/panel/admin">Dashboard</a>
      <a href="/panel/admin/sellers">Sellers</a>
      <a href="/panel/admin/settings">Settings</a>
    </nav>
    <label for="nav-toggle" class="nav-backdrop" aria-label="Close menu"></label>
  </header>
  <div class="page">
    <h1>System Settings</h1>
    <% if (typeof success !== 'undefined' && success) { %>
    <div class="card success-msg">Admin username/password updated.</div>
    <% } %>
    <% if (typeof error !== 'undefined' && error) { %>
    <div class="card error-msg" role="alert">
      <% if (error === 'current') { %>Current password is wrong.<% } else if (error === 'nochange') { %>Enter a new username and/or password to change.<% } else if (error === 'duplicate') { %>That username is already taken.<% } else { %>Update failed. Try again.<% } %>
    </div>
    <% } %>
    <div class="card">
      <h2>Admin credentials</h2>
      <p class="text-dim">Current username: <strong><%= typeof username !== 'undefined' ? username : '' %></strong></p>
      <form method="post" action="/panel/admin/settings" class="form">
        <div class="form-group">
          <label for="current_password">Current password</label>
          <input id="current_password" name="current_password" type="password" required autocomplete="current-password" placeholder="Enter current password">
        </div>
        <div class="form-group">
          <label for="new_username">New username</label>
          <input id="new_username" name="new_username" type="text" value="<%= typeof username !== 'undefined' ? username : '' %>" placeholder="Leave unchanged or enter new username" autocomplete="username">
        </div>
        <div class="form-group">
          <label for="new_password">New password</label>
          <input id="new_password" name="new_password" type="password" placeholder="Leave blank to keep current password" autocomplete="new-password">
        </div>
        <button type="submit" class="btn">Update credentials</button>
      </form>
    </div>
  </div>
</body>
</html>
