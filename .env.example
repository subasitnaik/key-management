# Server
PORT=3000

# Supabase — project URL + anon key (or service_role key for full access)
# Get from: Supabase Dashboard → Project Settings → API
# Project URL = Project URL, anon key = anon public (or use service_role secret for server)
SUPABASE_URL=https://xxxxxxxx.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# Session secret - CHANGE THIS in production (min 32 chars)
SESSION_SECRET=change-me-in-production

# Default seller slug when /connect/ has no slug in path
SELLER_SLUG=test

# Full base URL - REQUIRED in production (e.g. https://your-app.railway.app)
PANEL_URL=http://localhost:3000

# Bot tokens are set per seller in Master Admin, not here
